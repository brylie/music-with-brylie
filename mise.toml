[tools]
python = "latest"
uv = "latest"

[env._]

[env._.python]
venv = ".venv"

[tasks.setup]
description = "Install Python dependencies"
run = "uv sync"

[tasks.dev]
description = "Run development server"
run = "uv run python --version"

[prepare.uv]
auto = true
